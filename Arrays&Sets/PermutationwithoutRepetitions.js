//without repetition
function getPermutations(arr){
const permutations = []

if(options.length === 1){
    return [options];
}
const partialPermutations = getPermutations(options.slice(1));

for(var index=0; index<partialPermutations.length; index++){
    partialPermutation = partialPermutations[i]
}


}


const todoListItems = [

    'walk the dog',
    'clean the toilet',
    'buy groceries',
    'order food'
]